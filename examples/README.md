# Examples

This directory contains small scripts that use the installed package API.

Run examples from the repository root after installing the project:

```powershell
py -3.12 -m pip install -e .[dev]
py -3.12 examples/run_preview.py
```

Generated files are written under `outputs/examples/`.
